services:
  - type: web
    name: my-java-app
    env: docker
    plan: free
    repo: https://github.com/your-username/your-repo
    branch: master
    dockerfilePath: Dockerfile
    autoDeploy: true